all: zmq_core.so

clean:
	rm -f zmq.o zmq_core.so

zmq.o: zmq.cc
	@CXX@ @CXXFLAGS@ @SING_INCLUDES@ @SHOBJ_CFLAGS@ -c zmq.cc

zmq_core.so: zmq.o
	@CXX@ @LDFLAGS@ @SHOBJ_LDFLAGS@ -o zmq_core.so zmq.o @SING_LIBS@ -lzmq

install: zmq_core.so
@INSTALLATION@
.PHONY: all clean install
